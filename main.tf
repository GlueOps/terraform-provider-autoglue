terraform {
  required_providers {
    autoglue = {
      source = "glueops/autoglue/autoglue"
    }
  }
}

provider "autoglue" {
  # addr       = "https://api.example.com/api/v1"
  # api_key    = "..."
  # bearer     = "..."
  # org_id     = "..."
  # org_key    = "..."
  # org_secret = "..."
}